tips
目标主机:10.0.5.210
账户:root
密码:123
环境：linux debian
串口路径：/dev/ttyS4

任务1
    根据doc/assistant_protocol.md 协议和当前程序 生成一份测试用例md，测试用例表中包含每一帧的用例,要求覆盖除IAP升级功能和底层MCU主动上报的协议外其他所有的通讯协议字段和设定值，生成的测试用例存放在doc/
任务2
    2.1 根据测试用例和assistant_protocol.md编写测试python脚本，本地保存在debug_tools,复制到目标主机（如上面的tips）,进行执行
    2.2 每个协议帧都需要执行，并根据协议返回内容对比判定结果是否符合协议，完成测试后在对应的测试用例下标注打勾，如果测试失败则备注失败原因
    2.3 失败规则：1 返回字段不符合协议 2 返回超时（3S未响应）
    2.4 当前测试只作协议完整性测试，不包含硬件响应，和外部功能响应
    2.5 测试完成后根据测试结果生成测试报告，保存到doc/下
